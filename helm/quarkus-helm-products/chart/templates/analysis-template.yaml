apiVersion: argoproj.io/v1alpha1
kind: AnalysisTemplate
metadata:
  name: products-analysis-template
  labels:
    helm.sh/chart: quarkus-base-0.1.0
    app.kubernetes.io/name: products
    app.kubernetes.io/instance:  {{ .Release.Name }}
    app: products
    app.kubernetes.io/version: "1.0.0"
    app.kubernetes.io/managed-by: Helm
spec:
  metrics:
  - name: products-webmetric
    successCondition: result == 'UP'
    provider:
      web:
        url: "http://products-online.test.svc.cluster.local:8080/q/health/ready"
        jsonPath: "{$.status}"