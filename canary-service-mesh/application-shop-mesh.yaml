apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: shop
  namespace: openshift-gitops
spec:
  destination:
    name: ''
    namespace: gitops
    server: 'https://kubernetes.default.svc'
  source:
    path: helm/quarkus-helm-umbrella/chart
    repoURL:  https://github.com/pacaruso/cloud-native-deployment-strategies.git
    targetRevision: canary-mesh
    helm:
      valueFiles:
        - values/values-mesh.yaml
      parameters:
      - name: "global.namespace"
        value: gitops
      - name: "domain"
        value: "cluster-ttkl2.ttkl2.sandbox2306.opentlc.com"
  project: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true